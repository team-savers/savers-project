# 세이버스(SAVERS) 프로젝트 구조도

`AGENTS.md`, `개발자_가이드.md`, `리스크.md`, `구현_범위.md`에서 정리한 내용을 바탕으로 시스템 구조를 mermaid로 도식화한 문서입니다.

> ⚠️ **이 문서는 "무엇을 만들 것인가"(목표 아키텍처)입니다.** 지금 저장소에서 실제로 도는 것은
> 그 일부이며, 외부 의존은 전부 오프라인 스텁입니다. **"지금 무엇이 돌아가는가"는
> [워킹_스켈레톤_설명.md](워킹_스켈레톤_설명.md)** 에 모듈·함수 단위로 정리돼 있고, 각 단계를 직접
> 돌려 확인하는 절차는 [워킹_스켈레톤_점검.md](워킹_스켈레톤_점검.md)에 있습니다. 두 문서가
> 어긋나면 코드와 대조해 이 문서를 갱신하세요.
>
> 알림 채널은 [ADR-0005](../adr/0005-webpush-primary-channel.md)로 **PWA 웹푸시(FCM) 1차**가
> 확정됐습니다(카카오 알림톡은 예선 범위 제외). 아래 도식은 그 결정을 반영한 상태입니다.

## 1. 전체 파이프라인 (6단계)

경보 발생부터 개인 맞춤 알림 도달까지의 흐름입니다. 기획서의 "6단계 작동 파이프라인"을 그대로 반영했습니다.

```mermaid
flowchart LR
    A["① 사전 등록<br/>전화번호·프로필·거주 행정동<br/>(보호자/복지관/지자체 대리등록)"] --> B["② 재난 감지·상시 모니터링<br/>기상청·소방청·행안부 Open API 폴링"]
    B --> C["③ 대상자 매칭<br/>경보 행정동 ↔ 등록자 행정동"]
    C --> D["④ 위치 1회 확인<br/>알림 링크 클릭 시 Geolocation<br/>(세션 단위, 미저장)"]
    D --> E["⑤ RAG 메시지 생성<br/>Chroma 검색 → 가드레일 → HyperCLOVA X"]
    E --> F["⑥ 알림 발송<br/>PWA 웹푸시(FCM)<br/>문안 그대로 전달"]
    F --> G["Interactive Care 챗봇<br/>후속 Q&A 대기"]

    style D fill:#f9d,stroke:#333
    style E fill:#bbf,stroke:#333
```

> 목표: ①~⑥ 전 구간 종단 지연 30초 이내 (근거: [리스크.md](리스크.md) ③ 참고 — 낙관적 추정이므로 실측 필요).

## 2. 모듈별 시스템 아키텍처

기획서의 4개 모듈과 계층별 기술 스택을 데이터 흐름 순서로 배치했습니다.

```mermaid
flowchart TB
    subgraph M1["모듈 1 · 실시간 위치 매칭 엔진"]
        direction TB
        M1a["기상청·소방청·행안부 Open API<br/>(Python/FastAPI 스케줄러)"]
        M1b["행정동 코드 매칭 테이블"]
        M1c["브라우저 Geolocation API"]
        M1d["카카오맵 역지오코딩 API"]
        M1a --> M1b
        M1c --> M1d --> M1b
    end

    subgraph M2["모듈 2 · Advanced RAG 파이프라인"]
        direction TB
        M2a["국민행동요령 원문<br/>청킹 · 임베딩"]
        M2b[("Chroma 벡터 DB")]
        M2c["재난유형 + 취약계층 프로필<br/>조건부 시맨틱 검색"]
        M2a --> M2b --> M2c
    end

    subgraph M3["모듈 3 · 에이전틱 LLM 메시지 생성"]
        direction TB
        M3a["LangChain 오케스트레이션"]
        M3b["Guardrail 프롬프트<br/>(근거 인용 강제 · 환각 억제)"]
        M3c["HyperCLOVA X<br/>페르소나별 메시지 생성"]
        M3a --> M3b --> M3c
    end

    subgraph M4["모듈 4 · 무설치 사용자 터미널"]
        direction TB
        M4a["PWA 웹푸시(FCM)<br/>1차 채널"]
        M4b["딥링크 랜딩<br/>(위치 1회 확인)"]
        M4c["Interactive Care 챗봇<br/>TTS · 쉬운글 · 다국어 번역"]
        M4a --> M4b --> M4c
    end

    subgraph INFRA["인프라 · 운영 (전 모듈 공통 지원)"]
        direction LR
        I1["AWS 단일 VM · 서울 리전<br/>docker-compose (CPU 전용)"]
        I2["로그 기반 지연시간 계측"]
        I3["오프라인 캐시 예비모드"]
    end

    M1 --> M2
    M2 --> M3
    M3 --> M4
    INFRA -.지원.-> M1
    INFRA -.지원.-> M2
    INFRA -.지원.-> M3
    INFRA -.지원.-> M4
```

## 3. 위치 확인 → 알림 도달 임계 경로 (시퀀스)

지연시간 목표(30초)와 직결되는 구간만 떼어낸 시퀀스 다이어그램입니다.

```mermaid
sequenceDiagram
    participant U as 사용자(알림 링크 클릭)
    participant FE as PWA 클라이언트
    participant LOC as 위치 매칭 엔진
    participant RAG as RAG 파이프라인(Chroma)
    participant LLM as 가드레일 + HyperCLOVA X
    participant NOTI as 알림 발송(웹푸시/FCM)

    U->>FE: 알림 링크 진입
    FE->>LOC: Geolocation 좌표 1회 전송
    LOC->>LOC: 카카오맵 역지오코딩 → 위험구역/대피소 거리 계산
    LOC->>RAG: 재난유형 + 취약계층 프로필 전달
    RAG->>RAG: 시맨틱 검색 (국민행동요령 원문 발췌)
    RAG->>LLM: 검색된 근거 원문 전달
    LLM->>LLM: 가드레일 검증 (원문 밖 내용 차단)
    LLM->>NOTI: 개인 맞춤 메시지 생성 완료
    NOTI->>U: 웹푸시 발송 (문안 그대로)
    Note over U,NOTI: 목표 총 소요 30초 이내<br/>(웹푸시 발송 ~1s + LLM 생성 5~10s + 매칭·검색 여유분)<br/>⚠️ 전부 추정값 — 실측으로 교체
```

## 4. 저장소(Repo) 구조 제안

역대 수상팀 GitHub 구조 조사(원본 PDF는 public 레포 밖 보관)의 본질적 결론(AI를 인라인 호출이 아닌 **독립 배포 가능한 엔진**으로 두고 배포·운영 문서를 함께 정리)을 반영하되,
6인·해커톤 단기·Tech Lead 초반 집중 조건을 고려해 **단일 모노레포**로 운영합니다. AI(`apps/ai-engine`)는 모노레포 안에서 자체 Dockerfile·README로 독립 배포 가능한 엔진으로 유지합니다. (근거: [개발자_가이드.md](개발자_가이드.md) 7절, [ADR-0001](../adr/0001-monorepo.md))

```mermaid
flowchart TB
    REPO[("모노레포<br/>savers-project")]
    REPO --> APPS["apps/"]
    REPO --> PKG["packages/contracts<br/>OpenAPI 스펙 · 공용 스키마(SSOT)"]
    REPO --> INFRA2["infra/<br/>docker-compose · .env.example · AWS 프로비저닝"]
    REPO --> DOCS["docs/<br/>ADR · 설계·운영 가이드"]
    REPO --> E2E["e2e/<br/>앱 가로지르는 관통 테스트(QA 소유)<br/>HTTP 전용 · non-required CI"]
    REPO --> TOOL["notebooks/ · scripts/<br/>실험 기록 · 품질 게이트/레포 도구"]

    APPS --> FE["frontend<br/>PWA · 웹푸시 랜딩 · 챗봇 UX · 접근성"]
    APPS --> BE["backend<br/>FastAPI · 위치매칭 엔진 · 재난 API 연동"]
    APPS --> AI["ai-engine<br/>RAG · LangChain · 가드레일<br/>eval/ = 평가 하네스<br/>(자체 Dockerfile·README = 독립 배포)"]

    AI -.HTTP 계약으로만 연결<br/>파이썬 import 금지.-> BE
```

> 앱 간 결합은 `packages/contracts`의 HTTP 계약으로만 허용합니다. `apps/backend`와 `apps/ai-engine`이 서로를 파이썬 import 하는 순간 "독립 배포 가능한 AI 모듈"이라는 구조의 근거가 사라집니다.
> `e2e/`에도 같은 제약이 적용됩니다 — 관통 테스트는 기동된 서비스에 HTTP로만 접근하며, 앱 패키지를 import하지 않습니다.

## 5. 참고 문서 연결

- **지금 실제로 도는 코드 경로**: [워킹_스켈레톤_설명.md](워킹_스켈레톤_설명.md) (모듈·이음매·교체 지점)
- **단계별 동작 확인 절차**: [워킹_스켈레톤_점검.md](워킹_스켈레톤_점검.md) (명령 → 기대 출력 → 통과 기준)
- 각 모듈의 직접 구현 vs 외부 대체 범위: [구현_범위.md](구현_범위.md)
- 구조 설계 시 유의해야 할 리스크: [리스크.md](리스크.md)
- 팀 구성 및 비개발 배경 설명: [개발자_가이드.md](개발자_가이드.md)
- 역할별 실행 매뉴얼(누가·무엇을): [역할_가이드/](../역할_가이드/README.md)
- 시간축 실행 계획(언제·마일스톤·페이즈): [역할_일정/](../역할_일정/00-overall.md)
- 외부 승인·리드타임 체크리스트: [외부_승인.md](외부_승인.md)
- Claude Code 작업 시 기준 문서: [../../CLAUDE.md](../../CLAUDE.md)
