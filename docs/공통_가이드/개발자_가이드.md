# 세이버스(SAVERS) 프로젝트 참여자 가이드

이 문서는 팀원, 협업자가 프로젝트 배경과 방향을 빠르게 파악할 수 있도록 정리한 가이드입니다.

## 1. 프로젝트 현황

이 저장소에는 기획·설계 문서와 **모노레포 + 워킹 스켈레톤**이 있습니다. 재난 1건이 `수신 → 매칭 → 검색 → 생성(가드레일) → 세션 발급 → 웹푸시 발송 → 대피소 → 챗봇 → 보호자`까지 실제로 관통하지만, **외부 의존은 전부 오프라인 스텁**입니다 — 공공 API·국민행동요령 원문·HyperCLOVA X·FCM·대피소 API·등록자 저장소 어느 것도 아직 실물이 아닙니다([ADR-0007](../adr/0007-walking-skeleton.md)에 이음매와 소관자 표가 있습니다). 관통했다고 기능이 완성된 것이 아니며, 각자 자기 이음매를 교체하는 것이 P1 작업입니다. 관통 경로의 모듈·함수 단위 동작과 교체 지점은 [워킹_스켈레톤_설명.md](워킹_스켈레톤_설명.md)에 정리돼 있습니다.  
모든 기획·설계 문서는 `docs/` 아래에 있습니다. 설계 문서(`공통_가이드/`: `아키텍처.md`·[워킹_스켈레톤_설명.md](워킹_스켈레톤_설명.md)·`리스크.md`·`구현_범위.md`·`외부_승인.md`·`비용_산정.md`·[환경_세팅_가이드.md](환경_세팅_가이드.md)·[배포_절차.md](배포_절차.md)·[서버_운영.md](서버_운영.md)·[AWS_계정_발급_가이드.md](AWS_계정_발급_가이드.md)·[저장소_운영.md](저장소_운영.md)), 결정 기록(`docs/adr/`), 역할별 실행 매뉴얼([역할_가이드/](../역할_가이드/README.md)), 실행 타임라인([역할_일정/](../역할_일정/README.md)), 개정이 잦은 공유 문서([공유_문서/](../공유_문서/README.md))가 정리돼 있습니다.  
해커톤 제출 PDF(참가신청서·기획서)와 팀 내부 리서치 자료는 **이 저장소에 없습니다** — public 레포라 개인정보·내부 조사 자료를 올리지 않고 팀 공유 드라이브에 보관합니다([AGENTS.md](../../AGENTS.md)의 "Not in this repository" 참고).  
2026년 제8회 K-디지털 트레이닝 해커톤에 출품하는 팀 **세이버스(SAVERS)** 의 작업 공간입니다.  
핵심 마일스톤은 **예선 개시 2026-07-27**, **예선 제출본(실측 지표 반영) 2026-08-10 13:00**, **본선 9월 초**이며, 모든 페이즈(P1~P5)는 예선 개시일 7/27부터 시작합니다. 상세 일정은 [../역할_일정/00-overall.md](../역할_일정/00-overall.md)를 참고하세요.  
개발 환경은 [환경_세팅_가이드.md](환경_세팅_가이드.md)의 `bash scripts/setup-dev.sh` 한 줄로 맞춥니다(Windows는 `scripts\setup-dev.ps1`). 기능 구현은 아래 모듈·기술 스택 계획에 맞춰 이 스캐폴딩을 확장하는 방식으로 진행합니다.

## 2. 세이버스가 해결하려는 문제

세이버스는 스스로 판단·대피하기 어려운 사람들 — 안전취약계층(어린이·노인·장애인)과 언어 장벽이 있는 외국인 근로자 — 을 위한 재난 대응 보조 서비스입니다.

핵심 문제의식은 이렇습니다: **정보가 없는 것이 문제가 아니라, "정보를 개인의 행동으로 전환하는 능력"이 부재하다는 것**입니다.  
기존의 긴급재난문자(CBS)나 안전디딤돌은 모두에게 동일한 지역 단위 정보를 일방적으로 전달할 뿐입니다.  
세이버스는 등록된 사용자에게 재난 경보가 오면, 알림 링크를 여는 그 순간에만 현재 위치를 **1회** 확인(백그라운드 추적 없음, 세션 단위 사용·미저장)하고,  
가드레일로 통제된 개인 맞춤 행동지침을 **PWA 웹푸시(FCM)** 로 전달합니다([ADR-0005](../adr/0005-webpush-primary-channel.md)).

MVP의 1차 대상 재난 유형은 **호우·도시침수**입니다.

## 3. 아키텍처 (계획된 4개 모듈)

1. **실시간 위치 매칭 엔진**
    평상시엔 등록된 행정동으로만 매칭하고,  
    경보 발생 시에만 브라우저 Geolocation → 카카오맵 역지오코딩으로 위험구역 여부·대피소 거리를 계산합니다.
2. **Advanced RAG 파이프라인**
    행정안전부 국민행동요령을 청킹·임베딩해 Chroma 벡터 DB에 색인하고, 재난 유형+사용자 프로필 조건으로 검색합니다.
3. **에이전틱 LLM 메시지 생성**
    LangChain으로 오케스트레이션하고 HyperCLOVA X로 생성하되,  
    검색된 공식 원문만 근거로 답하도록 강제하는 가드레일 프롬프트를 적용해 환각을 억제합니다.
4. **무설치 사용자 터미널**
    **PWA 웹푸시(FCM)** 로 전달하며, 생성된 문안은 **변형 없이 그대로** 나갑니다  
    (템플릿 슬롯 치환 계층 없음 — [ADR-0005](../adr/0005-webpush-primary-channel.md)).  
    대화형 챗봇(Interactive Care)과 접근성 기능(TTS·쉬운 글·다국어 번역)을 제공합니다.

### 계층별 기술 스택

| 계층 | 기술 |
|---|---|
| 데이터 수집 | 기상청·소방청·행정안전부 재난안전데이터 공유 플랫폼 Open API, Python/FastAPI 스케줄러 |
| 위치 처리 | 브라우저 Geolocation API, 카카오맵 역지오코딩 API, 행정동 코드 매칭 테이블 |
| 지식베이스 | 행정안전부 국민행동요령 원문, Chroma 벡터 DB |
| 생성·제어 | LangChain 오케스트레이션, HyperCLOVA X, 가드레일 프롬프트 |
| 전달·접근성 | **PWA 웹푸시(FCM)** — 1차 채널, 카카오 알림톡은 예선 범위 제외([ADR-0005](../adr/0005-webpush-primary-channel.md)), TTS, 다국어 번역 |
| 인프라·운영 | AWS **단일 VM**(서울 리전 ap-northeast-2, CPU 전용 — HyperCLOVA X·임베딩이 외부 API라 **GPU 불필요**;<br>docker-compose로 백엔드·AI 엔진 구동, 관리형 이중화는 확장 시로 유보 → [ADR-0003](../adr/0003-single-vm-seoul.md);<br>PWA는 정적 빌드라 VM 밖 정적 호스팅(Vercel) → [ADR-0008](../adr/0008-frontend-static-hosting.md)), 로그 기반 지연시간 계측, 오프라인 캐시 예비모드 |

## 4. 반드시 지켜야 할 설계 원칙

아래 원칙들은 기획 단계에서 이미 리스크 대응 차원에서 확정된 것으로, 개발 편의를 위해 임의로 바꾸면 안 됩니다.

- **상시 위치 추적 금지**: 위치는 알림을 여는 시점에 딱 1회만 읽고, 세션 단위로만 사용하며 저장하지 않습니다.
- **근거 기반 생성 필수**: LLM은 반드시 검색된 국민행동요령 원문에 근거해서만 답해야 합니다.  
    가드레일은 환각 억제를 위한 핵심 장치이며, 그 on/off 효과 자체가 측정 지표이므로 편의상 우회하거나 제거하면 안 됩니다.
- **당사자에게 설치 부담 없음**: 등록·설정은 당사자가 아니라 보호자·복지관·지자체가 대신하도록 설계되어 있습니다.  
    취약계층 당사자에게 앱 설치나 직접 설정을 요구하는 기능은 이 원칙과 충돌합니다.
- **오프라인/장애 대응 필수**: 공공 재난 API가 지연되거나 장애가 나면,  
    조용히 실패하는 대신 사전 캐시된 대피소, 행동요령으로 전환되는 예비모드가 있어야 합니다.
- **최소 수집·암호화**: 위치, 장애 여부 등 민감정보는 최소한만 수집하고 암호화합니다.  
    새로운 사용자 필드를 추가할 때도 이 기준을 하드 요구사항으로 취급해야 합니다.

## 5. 품질 검증 지표 (사람 채점이 아니라 코드로 재현 가능해야 함)

이 팀의 차별화 전략은 품질을 **설문이 아닌 재현 가능한 자동 채점 스크립트**로 증명하는 것입니다.  
평가 로직을 구현할 때는 다음을 기준으로 삼습니다.(**제시된 숫자는 임의의 기준입니다**)

- **알림 도달 속도**: 재난 API 수신 → 개인 맞춤 알림 발송까지의 종단 지연시간. 목표 30초 이내, 100건 평균·p95(95% 성공)로 측정.
- **근거 일치율**: 생성 메시지와 국민행동요령 원문의 일치도. 목표 85% 이상, 30 ~ 50건 평가셋 기준.
- **환각 억제율**: 동일 평가셋을 가드레일 on/off로 비교. 목표 50% 이상 발생 건수 감소.
- **메시지 명확성**: 행정용어 치환율 90% 이상, "실행 가능한 단일 행동" 포함률 100% (국립국어원 쉬운 공공언어 기준).  
    규칙 기반 채점 + LLM 교차 채점(생성에 쓰지 않은 별도 모델로 채점해 자기평가 편향 배제)으로 확인.
- **재난 이력 리플레이**: 과거 호우 특보 이력을 실제로 재생해, 합성 테스트가 아닌 실제 발령 조건에서 지연시간·근거 일치율을 검증합니다.

위 수치들은 모두 가설 목표치이며, 예선 기간 중 실제 프로토타입 실측값으로 교체됩니다.  
저장소에 실측 데이터가 쌓이면 이 문서의 숫자를 최종 근거로 삼지 마세요.

## 6. 팀 구성 및 담당 영역

| 이름 | 역할 | 담당 |
|---|---|---|
| 안은남 | PM | 전체 마일스톤·통합 관리, 본선 발표·시연 총괄 |
| 신호정 | Tech Lead | 시스템 아키텍처·모듈 간 계약 확정, Walking Skeleton(전체 흐름 관통) 구현,<br>가드레일 프롬프트 설계 원칙·초기 버전, 레포 스캐폴딩·CI·평가 하네스 골격. 산출물 리뷰, 자문 |
| 김소원 | AI/RAG Engineer | 국민행동요령 전처리·청킹, Chroma 인덱싱, 시맨틱 검색 정교화, 근거 일치율 측정 |
| 최혜리 (겸임) | Frontend/UX | PWA·웹푸시(FCM) 연동, 챗봇 UI/UX, 접근성(다국어·쉬운 글·음성) 구현, 가독성 지표 계측 |
| 김도혁 | Backend/Infra | 기상청·소방청 API 연동, 위치 매칭 엔진, FastAPI 백엔드, 인프라 이중화, 오프라인 예비모드 |
| 최혜리 | QA/Security | 민감정보 최소수집·암호화, 가드레일 검증, 웹푸시 대체경로, E2E 테스트, 시연 자료 |

## 7. 저장소 구조 컨벤션 (모노레포)

팀이 직접 수행한 역대 수상팀 GitHub 구조 조사(원본 PDF는 public 레포에 올리지 않고 팀 공유 드라이브에 보관 — [AGENTS.md](../../AGENTS.md)의 "Not in this repository" 참고)의  
**본질적 결론**은 **"AI를 인라인 API 호출이 아니라 독립적으로 배포 가능한 엔진/모듈로 두고, 배포·운영 문서까지 README에 정리하라"** 입니다.

우리 팀은 **6인 · 해커톤 단기 · Tech Lead 초반 집중** 조건이므로,  
멀티레포의 관리비용(문서·인프라 중복, cross-repo PR 조율, 온보딩 부하)과 통합 부채가 이득보다 큽니다.  
따라서 **단일 모노레포**를 채택하되, AI는 그 안에서 **독립 배포 가능한 서비스**로 유지해 수상작의 본질 조건을 충족합니다.

```
savers-project/            # 단일 레포 (모노레포) — github.com/team-savers/savers-project
  apps/
    frontend/              # PWA — React+Vite+TS, 웹푸시(FCM) + 통합 서비스워커
      package.json         #   백엔드 연동 전이라 mock 어댑터가 기본(VITE_USE_MOCK=true)
    backend/               # FastAPI (데이터 수집·위치 매칭)
      pyproject.toml       #   src/api(얇은 라우터) + src/backend_core(도메인)
    ai-engine/             # 독립 배포: 자체 Dockerfile + README (RAG·LangChain·가드레일)
      pyproject.toml       #   src/ai_engine
      eval/                #   평가 하네스 — 골든셋·지표 함수 (채점 대상과 같은 배포 단위)
  packages/
    contracts/             # OpenAPI 스펙 + 공용 스키마/타입 (단일 진실 공급원, SSOT)
  infra/                   # docker-compose, .env.example, AWS 프로비저닝
  e2e/                     # 앱 가로지르는 관통 테스트 (QA 소유, HTTP 전용, non-required CI)
  notebooks/               # 실험 기록 (배포 대상 아님 — 검증되면 apps/<app>/src 로 이전)
  scripts/                 # setup-dev.sh/.ps1(환경 점검·복구) · run-tests.sh(품질 게이트)
                           #   · setup-github.sh(레포 하드닝)
  docs/                    # ADR(docs/adr/), 설계·운영 가이드
  pyproject.toml           # ⚠️ ruff 설정 전용 — 설치 가능한 패키지가 아님
  .github/workflows/       # pre-commit + 앱 매트릭스 CI (required check 5종)
  CODEOWNERS               # 폴더별 소유자
```

- **문서·인프라·env 공유**: `infra/`·`docs/`에 한 곳으로 모아 중복·드리프트 제거.
- **관리 부하 최소화**: `docker-compose up` 한 번으로 전체 로컬 구동, 단일 PR로 계층 걸친 변경을 atomic 반영.
- **경계 유지**: `CODEOWNERS` + 브랜치 보호로 소유권 분리.
- **CI 구성**: 워크플로는 **required(필터 없음) / non-required(paths 필터)** 두 갈래로 나뉩니다. required status check에 paths 필터를 걸면 해당 경로를 건드리지 않은 PR에서 체크가 생성되지 않아 머지가 영구 블록되기 때문입니다.

| 워크플로 | 잡 | required | 트리거 |
|---|---|---|---|
| `ci.yml` | `Pre-commit hooks`(훅 전량 + gitleaks 전체 트리 스캔) · `Lint & Type Check (backend/ai-engine)` · `Unit tests (backend/ai-engine)` | ✅ 5종 | 모든 PR |
| `frontend-ci.yml` | 프론트 lint/typecheck/build | ❌ | `apps/frontend/**` |
| `docker-build.yml` | 이미지 빌드 + `/health` 스모크, compose config 검증 | ❌ | `apps/*/Dockerfile`·`src/**`·`infra/**` |
| `contracts-check.yml` | OpenAPI 스펙 검증 | ❌ | `packages/contracts/**` |
| `e2e.yml` | 종단 관통 테스트 (스택 기동 → HTTP 관통) | ❌ | `e2e/**`·`apps/**`·`infra/**` |
| `notebook-check.yml` | 노트북 출력 셀 제거 확인 | ❌ | `**.ipynb` |

  non-required 워크플로 4종은 **대상 파일이 아직 없으면 전 단계를 건너뛰도록** 작성돼 있어, 프론트엔드·compose·OpenAPI 스펙이 생기는 순간 별도 설정 없이 활성화됩니다. required 잡을 추가·개명할 때는 `scripts/setup-github.sh`의 `contexts` 배열을 **같은 PR에서** 함께 고치세요 — 이름이 어긋나면 모든 PR이 생성되지 않는 체크를 기다리며 블록됩니다.
- **수상작 본질 충족**: `apps/ai-engine/`은 자체 Dockerfile·README로 "독립 배포 가능한 엔진"임을 증명.
